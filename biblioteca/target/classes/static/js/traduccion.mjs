

'use strict'





/*


        function translatePage(language) {
            gapi.client.translate.translations.list({
                q: $('body').text(),
                source: 'auto',
                target: language,
            }).then(function(response) {
                $('body').html(response.result.translations[0].translatedText);
            }, function(error) {
                console.error('Error translating page', error);
            });
        }
      */